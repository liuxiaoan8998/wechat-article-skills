# v3.0.12-新增改进点（2026-04-29）

## 1. Base查重防重复机制

### 问题
用户可能多次发送相同的微信文章链接，导致Base中出现重复记录。

### 解决方案
`lark-cli base +record-search` API 需要 `keyword` + `search_fields` 参数，不适用于精确链接查重。改用 `record-list` 全量拉取+本地匹配：

```python
import subprocess, json

base_token = "YOUR_BASE_TOKEN"  # 从环境变量或配置读取
table_id = "YOUR_TABLE_ID"

# 1. 拉取全部记录
cmd = f'lark-cli base +record-list --base-token {base_token} --table-id {table_id} --as bot'
result = subprocess.run(cmd, shell=True, capture_output=True, text=True)

# 2. 解析响应，构建链接映射
if result.returncode == 0:
    response = json.loads(result.stdout)
    if response.get('ok'):
        records = response['data']['items']
        existing = {}
        for r in records:
            fields = r.get('fields', {})
            url = fields.get('文章链接', '')
            if url:
                existing[url] = {
                    'record_id': r['record_id'],
                    'article_id': fields.get('文章ID', ''),
                    'title': fields.get('文章标题', ''),
                    'status': fields.get('文章状态', '')
                }

# 3. 查重判断
target_url = metadata.get('url', '')
if target_url in existing:
    dup = existing[target_url]
    print(f"⚠️ 文章已存在于Base中！")
    print(f"   记录ID: {dup['record_id']}")
    print(f"   文章ID: {dup['article_id']}")
    print(f"   状态: {dup['status']}")
    # 跳过创建，结束流程
```

### 处理逻辑
- 发现重复 → 保留原始记录，不创建新记录
- 清理本次提取产生的输出目录，避免占用磁盘空间
- 最终报告中显示重复检测结果

---

## 2. 精简HTML正则方案（无需BeautifulSoup）

### 问题
环境中可能缺少 `beautifulsoup4`，导致提取 `js_content` 失败。

### 解决方案
使用正则提取，不依赖第三方库：

```python
import re

# 从原始HTML中提取 js_content
pattern = r'<div[^>]*id=["\']js_content["\'][^>]*>(.*?)</div>\s*</div>'
match = re.search(pattern, html, re.DOTALL)

if match:
    content_html = match.group(1)
    
    # 清理冗余属性（保留 src/href/data-src）
    content_html = re.sub(r'style=["\'][^"\']*["\']', '', content_html)
    content_html = re.sub(r'class=["\'][^"\']*["\']', '', content_html)
    
    # 构建完整HTML
    clean_html = f"""<!DOCTYPE html>
<html><head><meta charset="UTF-8">
<style>
img{{max-width:100%;height:auto;display:block;margin:10px 0}}
p{{margin:8px 0;line-height:1.6}}
body{{font-family:-apple-system,BlinkMacSystemFont,sans-serif;padding:16px}}
</style></head>
<body><div id="js_content">{content_html}</div></body></html>"""
    
    # 文件大小从 ~3.7MB 降至 ~2-5KB
```

### 关键要点
- 正则表达式必须使用 `re.DOTALL` 模式
- 保留的属性：`src`, `href`, `data-src`, `alt`
- 移除的属性：`style`, `class`, `id`（除了 `js_content` 本身）

---

## 3. 长图OCR自动处理

### 问题
超长图文章（如 1080x14232）需要多次OCR识别，手动处理效率低。

### 解决方案
工具已自动处理：

1. **自动切片**：检测图片高度 > 2000px 时自动切片为多段（如 8 段）
2. **自动OCR**：每个切片调用 RapidOCR 识别
3. **自动回填**：OCR 结果回填至 `article-ocr.md` 的 `[段N]` 标记中

### AI分析阶段
AI 分析时直接读取 `article-ocr.md` 中的 OCR 结果，无需额外调用 `vision_analyze`：

```python
# 直接读取OCR内容
with open(article_dir / 'article-ocr.md', 'r', encoding='utf-8') as f:
    ocr_content = f.read()

# 基于OCR内容生成结构化总结
# 回填第四部分："完整文字内容（原文 + OCR + 二维码）"
```

---

## 4. 重复文章报告格式

当检测到重复文章时，最终报告应包含：

```markdown
## ⚠️ 重复文章检测

| 项目 | 内容 |
|------|------|
| **文章标题** | xxx |
| **公众号** | xxx |
| **文章ID** | xxx |
| **记录ID** | xxx |
| **文章状态** | xxx |
| **操作** | 已跳过，未创建新记录 |
```

---

## 5. 字段填充率目标

- 目标：**21/21字段 100%填充**（不含系统自动生成的 ID/最后更新时间）
- 缺失信息统一填充 `"/"`，不能留空
- 同步前必须做字段完整性校验

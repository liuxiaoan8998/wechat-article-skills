# v3.0.12 改进点

## 1. 飞书Base自动查重
- 提取前检查是否已存在相同标题
- 已存在则跳过，避免重复提取
- 返回已有记录的完整路径

## 2. 精简HTML生成
- 内置于main.py，无需额外脚本
- 从 3.7MB 缩至 ~17KB
- 保留完整格式和图片链接

## 3. 长图OCR支持
- 检测图片高度 > 2000px
- 自动切片后OCR识别
- 检测裁剪时的高度阈值：小于500px屏幕时跳过
- 切片存储在 `slices/` 目录
